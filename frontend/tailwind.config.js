/**
 * CCS Profiling — Tailwind v4 uses CSS-first theme in `src/index.css` (`@theme`).
 * This file declares content paths for tooling; colors:
 *   Vibrant Orange #FF6B00 → `ccs-orange`, `ccs-orange-dark`, `ccs-orange-soft`
 *   Pure White #FFFFFF → `white` (default) / glass surfaces use `white/70`
 *   Ink / muted text → `ccs-ink`, `ccs-muted`
 */
export default {
  content: ['./index.html', './src/**/*.{js,jsx,ts,tsx}'],
}
